# requirements.lock — pinned direct dependencies for reproducible image builds.
#
# requirements.txt is the human-readable source of truth (what we depend on).
# This lock is what the Docker image installs (exact versions → same image every build).
# Versions captured from the working Python 3.13 environment.
# Regenerate after bumping requirements.txt:  pip install -r requirements.txt && pip freeze
fastapi==0.135.2
uvicorn[standard]==0.42.0
requests==2.32.5
python-dotenv==1.2.1
scikit-learn==1.8.0
numpy==2.4.3
openai==2.30.0
supabase==2.28.3
python-jose[cryptography]==3.5.0
pika==1.3.2
